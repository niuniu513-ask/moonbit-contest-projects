---
name: moonbit-contest-projects
description: Plan, build, review, and package independent MoonBit projects for a hackathon or ecosystem contest. Use when the user wants contest-ready local source folders, proposals, reproducible demos, tests, or acceptance checks; respect an explicit local-only scope.
---

# MoonBit 参赛项目开发与交付

将选题、实现、申报材料和本地验收串成一个可复查的工作流程。先判断用户要的是选题、开发、审查、申报书，还是最终目录整理；只执行当前请求涉及的阶段。不要因“参赛”二字自动初始化 Git、上传仓库、报名或发布 Mooncakes。

## 1. 确认规则与范围

- 打开用户指定的当期赛事章程、官网和申报样本；记录版本或查看日期。章程优先于旧赛事指南、样本及既有项目文案。页面无法读取时说明缺口，不猜测硬门槛。
- 将条件分成：本地可验证的项目质量、需要外部状态的报名/公开仓库/提交历史/CI 实际运行/平台发布。用户明确排除的外部事项不执行，也不把它们算作本地代码缺陷；最终结论须说明“本地就绪”不等于“官方验收通过”。
- 核对个人或团队的项目数量限制。多个技术上独立的目录不自动意味着同一参赛人可同时申报多个项目。
- 先盘点工作区、已有项目、未提交改动和工具链；保留用户已有源码、Git 历史及资料。环境与原始版本要整理时，优先移至经核对的工作区外位置；删除只针对明确确认可丢弃的内容。自动审批拒绝时停止该删除并如实报告。

## 2. 选题与查重

- 为每个项目写清真实使用者、输入与输出、最小可用能力、明确不做的范围以及下一阶段可扩展方向。项目应能独立运行和复用，不靠另一个参赛目录的私有源码才能使用。
- 搜索 GitHub、Mooncakes 和相关社区的名称及功能关键词，查看结果中的实际功能与维护状态。记录查询词和日期；搜索为空只能说明“未在该范围发现”，不能保证全网唯一。发现成熟同类项目时说明差异或重新选题。
- 多项目时检查题目、模块名、依赖、源码和测试的重合度；共享基础知识可以，机械拆分同一功能或重复提交不能当作多个独立项目。

## 3. 开发独立项目

- 每个项目一个独立文件夹和 `moon.mod`，优先沿用当前 MoonBit 工具链认可的 `moon.mod`/`moon.pkg` 语法。文件夹短名、模块末段、README 标题、申报书项目名称保持一致；`README.md`、`LICENSE` 等使用标准文件名。
- 交付可调用的 MoonBit API 和真实核心逻辑，覆盖正常输入、边界和错误处理。定义诚实的能力边界：例如“结构预检”不宣称信任验证，“码字层”不宣称可扫描条码。
- 配齐根目录许可证、README（目标、安装前提、调用示例、可复现命令、限制）、至少一个 `moon run` 示例、核心测试，以及覆盖 check/build/test 的 CI 配置。CI 配置存在不等于线上 CI 已通过。
- 参考规范、第三方代码、测试数据和样例要注明来源与许可证；不要复制来源不明的代码或真实敏感数据。生成接口文件时运行 `moon info`，使 `.mbti` 与最终模块名一致。
- 若用户只要本地开发，可保留未来使用的 CI 配置，但不创建仓库、提交、远程、报名或发布操作。

## 4. 写申报书

- 依当期样本组织标题和字段，参赛人、联系方式、仓库链接等由用户填写的信息留空。优先控制在当期规定的篇幅内。
- 写项目简介、方向与生态位置、至少当期规则要求数量的完整使用场景、核心功能、实现路径、预期交付、原创/移植/参考说明及许可证。每个场景说明谁拿什么输入、得到什么结果，避免同义改写凑数量。
- 承诺仅写已实现或明确标成计划的能力；与 API、测试、README 对照。说明不支持的协议部分和安全边界。文字具体自然，避免空泛的“赋能”“打造”及重复模板句。

## 5. 本地验收

- 在每个项目根目录分别运行 `moon version --all`、`moon check --deny-warn --target all`、可用目标的 `moon build` 与 `moon test --deny-warn`、README 中的示例命令；依风险覆盖 JS、Wasm、WasmGC。Native 失败时区分系统编译器/runtime 故障与项目代码错误。
- `moon fmt` 与 `moon info` 后确认源码和生成接口没有意外变化。对照申报书逐项检查实际功能，并审阅关键格式、金额、时间、编码或安全边界；测试通过不能替代行为审查。
- 给出项目分别的本地结论，列阻断问题、尚未验证的条件和准确命令结果。不要声称仓库 CI、Mooncakes 或官方审核通过，除非实际取得证据。

## 6. 整理交付目录

- 对照源目录与交付目录的相对文件清单，确保 `.mbt`、`moon.mod`、`moon.pkg`、示例、测试、README、申报书、许可证和 CI 配置无遗漏；区分生成接口与可删除的 `_build` 缓存。改名后更新所有模块导入与测试引用，再重新构建和测试。
- 若用户要求根目录只放项目，最终核对根级项目数量、名称、隐藏目录及缓存。工具链、安装包、旧版本和用户原有 Git 历史的去向要可追踪；不要为目录整洁破坏仍需使用的工具链或删除未获准丢弃的数据。
- 最终交代每个项目的绝对路径、包含的交付内容、已通过的本地验证及未完成的外部验收条件。用户要求不上传时，明确确认没有初始化/提交/上传/发布。
